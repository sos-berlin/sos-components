<?xml version="1.0" encoding="utf-8"?>
<param xmlns="http://www.sos-berlin.com/schema/js7_job_documentation_v1.1"
       xmlns:xhtml="http://www.w3.org/1999/xhtml"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       name="credential_store_entry_path" required="false">
	<note>
		<p>
			The argument specifies a default path to be used when accessing entries in a KeePass credential store.
            The syntax is cs://path/entry. If this argument is used then any subsequent references to a credential store
            do not have to specify the full path to an entry, but the property only that is retrieved from the credential store,
            for example cs://@password, with the full path being cs://path/entry@password.
		</p>
	</note>
</param>
