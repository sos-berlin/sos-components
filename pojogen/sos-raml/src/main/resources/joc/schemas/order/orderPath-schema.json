{
  "title": "orderPath",
  "id": "schemas/order/orderPath",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "javaType": "com.sos.joc.model.order.OrderPath",
  "type": "object",
  "required": [
    "workflow"
  ],
  "properties": {
    "workflow": {
      "$ref": "../common/path-schema.json"
    },
    "orderId": {
      "description": "if orderId undefined or empty then all orders of specified workflow are requested",
      "type": "string"
    }
  }
}
