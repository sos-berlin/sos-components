{
  "title": "jobscheduler",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "javaType": "com.sos.joc.model.jobscheduler.JobScheduler",
  "type": "object",
  "extends" : {
  	"$ref": "jobscheduler_p-schema.json"
  },
  "oneOf": [
    {
      "description": "if state.text=unreachable",
      "required": [
        "id",
        "surveyDate",
        "jobschedulerId",
        "url",
        "state"
      ]
    },
    {
      "required": [
        "id",
        "surveyDate",
        "jobschedulerId",
        "host",
        "url",
        "state",
        "version",
    	"os",
    	"timeZone",
    	"clusterType",
        "startedAt"
      ]
    }
  ],
  "properties": {
    "state": {
      "$ref": "state-schema.json"
    }
  }
}
